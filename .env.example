# Azure OpenAI configuration
AZURE_OPENAI_API_KEY=""     # Key-based authentication API key
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT_NAME=your-deployment-name

# MCP server environment variables can be added here if needed

# GitHub MCP server token
GITHUB_PERSONAL_ACCESS_TOKEN=""

# Brave Search MCP server API key
BRAVE_API_KEY=""

# Security settings
API_KEY=""                  # Optional API key for authentication (leave empty for development)
ENABLE_RATE_LIMITING=true   # Enable rate limiting
LOG_LEVEL=INFO             # Logging level (DEBUG, INFO, WARNING, ERROR)
ENVIRONMENT=development     # Environment (development, production, testing)

# CORS settings (comma-separated list)
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Optional: Override default MCP server URLs
# MCP_GITHUB_URL=http://mcp-github:3000
# MCP_BRAVE_SEARCH_URL=http://mcp-brave-search:3000
# MCP_SQLITE_URL=http://mcp-sqlite:3000
